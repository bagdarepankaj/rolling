<h1>Editing project</h1>

<%= render "form", project: @project, organization: @organization %>

<br>

<div>
  <%= link_to "Show", organization_project_path(@organization, @project) %> |
  <%= link_to "Back", organization_projects_path(@organization) %>
</div>
